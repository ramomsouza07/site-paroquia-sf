[build]
  command = "rm -rf node_modules && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.23.2"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200